# Copy to .env.local and fill in real values.
# Web3Forms access key (https://web3forms.com) — inlined into the client
# bundle at build time; set it in Cloudflare build settings for deploys.
NEXT_PUBLIC_WEB3FORMS_KEY=your-web3forms-access-key
