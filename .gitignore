# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# Dependencies
node_modules
.pnp
.pnp.js

# Local env files
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Testing
coverage

# Turbo
.turbo

# Vercel
.vercel

# Build Outputs
.next/
out/
build
dist


# Debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Misc
.DS_Store
*.pem
storybook-static/

# Raw photography exports (unprocessed originals, not for the repo)
/photography/
# Local text-to-speech toolchain (see scripts/tts/README.md).
# The venv and the ~350 MB Kokoro weights are re-fetched by setup, never committed.
scripts/tts/.venv/
scripts/tts/models/
scripts/tts/samples/

# Essay narration: generated locally, published to R2 by scripts/tts/publish.sh.
# Kept out of git so ~24 MB of mp3 never enters history.
apps/web/public/audio/

# Wrangler local state (R2 emulator, dev cache)
.wrangler/

# Python bytecode
__pycache__/
*.pyc
